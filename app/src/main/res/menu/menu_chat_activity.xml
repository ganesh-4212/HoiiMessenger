<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/share_selected_chat"
        android:title="Share"
        android:actionLayout="@layout/activity_chat"
        android:icon="@drawable/ic_share_black_24dp"
        android:visible="true"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/delete_selected_chat"
        android:title="Delete"
        android:actionLayout="@layout/activity_chat"
        android:icon="@drawable/ic_delete_white_24dp"
        android:visible="true"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/upload_image"
        android:title="Upload image"
        android:actionLayout="@layout/activity_chat"
        android:icon="@drawable/ic_attachment_white_24dp"
        android:visible="true"
        app:showAsAction="ifRoom"
        />
</menu>